---
alwaysApply: true
---

- Do not use Tailwind’s default typography sizing/weight/spacing utilities (`text-[size]`, `font-*`, `leading-*`, `tracking-*`) directly.
- Colour utilities (e.g., `text-foreground`, `text-muted-foreground`) are allowed.
