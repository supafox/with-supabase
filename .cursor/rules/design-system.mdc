---
alwaysApply: true
---

- Only use project-defined typography utilities (e.g., `heading-*`, `copy-*`, `button-*`, `label-*`).
- Do not use Tailwind's default `text-*`, `font-*`, `leading-*`, or `tracking-*` classes directly.
